<!DOCTYPE html>
<html>
  <head>
    <meta charset="utf-8">
    <title>Web Guesser</title>
    <style>
    <% if message == "Way too high!" || message == "Way too low!"%>
      body {
        background: #da2c38;
      }
    <% elsif message == "Too low!" || message == "Too high!" %>
    body {
      background: #ff5e69;
    }
    <% else %>
    body {
      background: #69dc9e;
    }
    <% end %>
    </style>
  </head>
  <body>
    <% if message == "That's correct!" %>
    <p>
      The SECRET NUMBER is <%= number %>
    </p>
    <% end %>
    <p>
      <%= message %>
    </p>
    <form>
      <input type="text" name="guess" />
      <input type="submit" value="Guess!" />
    </form>
  </body>
</html>
